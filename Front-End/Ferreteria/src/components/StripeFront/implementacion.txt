import { BrowserRouter, Routes, Route } from "react-router-dom"
import Success from "./pages/Success"

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/success" element={<Success />} />  Crear esto asi el servidor manda a este el resumen 
      </Routes>
    </BrowserRouter>
  )
}